# MEMORY

## Project

- Project name: New project 2
- Date initialized: 2026-06-20
- Visible stack: Vite/TypeScript web app with client, server, shared, pnpm, Render config, and GitHub Pages-style static build artifacts.

## Working Notes

- Follow `AGENTS.md` for ArtX test release workflow before any test environment publishing.
- Single-branch change isolation rule: frontend and backend share `feature/interaction-framework`, so every task must be scoped to the user's explicit request. Check branch and `git status` before editing, preserve unrelated dirty files, never stage `.env`/backup/temp/build artifacts, avoid unrelated refactors or formatting, and commit only task-required files plus focused tests. Final status should state changed files, unrelated/sensitive areas left untouched, commit hash if any, and which online environments are updated.
- Model selector display rule: user-facing model selectors should show the real model version IDs available to the current configured provider key, such as `gpt-image-2`, `gpt-image-2-4k`, or `gemini-3.1-flash-image`; do not hide them behind product-level names when the user needs precise model control.
- Image model catalog description rule: model-selector descriptions should be user-facing Chinese phrases of no more than 15 characters, must avoid price-tier wording such as `高价`/`中价`/`低价`, and should describe value/quality plus use cases such as portraits, scenes, anime, or film.
- Image2 model selector display rule: keep provider IDs such as `og-image2-low`, `og-image2-medium`, and `og-image2-high` for API calls, but display them globally as `image2 low`, `image2 medium`, and `image2 high`; image model rows use white line-style icons and keep the description 2px below the model label.
- Community QR rule: the left-sidebar `加入社群` dialog should use the stable `/api/community/wechat-group-qr/image` endpoint or `VITE_WECHAT_GROUP_QR_URL` for WeChat group QR images, with the bundled QR only as a fallback; this avoids frontend code changes when the QR rotates every 7 days.
- Log lookup rule: when the user says "查看日志" in this workspace, read `docs/dev-log.md` by default.
- Development log rule: when the user says "添加日志", "记一下日志", or asks to log a task, append the record to `docs/dev-log.md` using the established template; AI-related records must include `generationId`, `backendTaskId`, and `providerTaskId` or explicitly mark missing IDs as `N/A`.
- Credential values are not stored here; only credential locations may be recorded if needed.
- Global capability rule: when work affects shared UI, cross-page visual states, shortcuts, clipboard actions, delete, undo/redo, auth, requests, cache, logging, internationalization, or other common behavior, use `$global-capabilities` for an evidence-based audit first. Read `docs/global-capabilities.md` when present; unregistered components stay local by default, and repeated implementations require impact reporting and user confirmation before global refactoring.
- Smart commerce canvas rule: the canvas-top `智能电商产品` panel owns platform, market, category, placement, template, risk, resolution, and 1-9 image selection in one floating workflow. Keep versioned commerce rules in `shared/cross-border-commerce-agent.ts`, load them through `/api/cross-border-commerce/*`, and reuse the existing smart-product generation/canvas placement path. Requests above the provider limit must batch at four images and normalize every result to the selected 2K/4K platform aspect ratio; the commerce entry must also allow all nine placeholders/results while other derived AI operations retain their four-image default. After compose, show the audit ID, editable-copy suggestions, related export sizes, and generation summary, and persist this commerce context on result nodes; do not change unrelated AI provider capabilities.
- Product model: users pay for compute, compute is converted into platform credits/points, and admin tooling should track feedback, payments, accounts, and quota/credit operations.
- Admin prototype should include a top-right notification center for urgent operations such as payment exceptions, runtime errors, integration latency, and credit/risk events.
- Admin backend is connected at `/admin-prototype`, but it must not be exposed in the main website navigation; production access should use an independent admin subdomain such as `admin.*` or `VITE_ADMIN_HOST`.
- Formal admin access uses role-based permissions: ordinary users are `viewer`, default `09bee` is `super_admin`, and admin routes require `admin:access` instead of only checking login.
- Admin data integration rule: `/api/admin/*` now uses `server/admin-store.ts` as the MVP operating data source for users, orders, credits, AI tasks, providers, feedback, alerts, risk events, audit logs, and plans; all write operations must append audit logs and must never expose credential values.
- Billing integration rule: membership purchase is no longer only a local UI mock; `/api/billing/summary`, `/api/billing/orders`, and `/api/billing/orders/:orderId/pay` now create server-side orders, confirm payment, issue credits into the admin ledger, and keep `/admin-prototype` overview/orders/credits aligned with the same source of truth.
- Billing success feedback rule: subscription and recharge payment completion should return to an in-site success confirmation modal, not only a toast; the modal uses the prompt-send green `#C5ED47` as the top status bar and button color, shows the subscribed package or recharge amount/credits, and requires an explicit confirmation button.
- Admin manual credit gift rule: positive `/api/admin/credits/adjust` operations should create a user-facing unread gift notification with the copy `您好，您已收到系统为您赠送的 X 积分作为感谢。`; the global frontend notification polls `/api/billing/credit-notifications`, confirms through `/ack`, and broadcasts `artx:credits-updated` so every mounted `TopBar` refreshes the balance.
- Global toast alignment rule: bottom-centered Sonner toast defaults to the former expanded size, about 360px wide and 60px tall; text is centered when the full message fits within the toast content width, and switches to left-aligned two-line display when the measured text exceeds that width.
- Global toast negative state rule: bottom Sonner toasts whose type is `error` or whose title/description contains failure, error, unavailable, invalid, timeout, unable-to-connect, or equivalent Chinese negative wording should be marked as negative globally, show a leading `!`, use a deep red 30% transparent background, and keep prompt text white.
- Billing page layout rule: subscription and recharge cards must keep fixed dimensions across hover/click/selected states; the three visible subscription plans remain clickable as `Lite 入门版`, `Pro 专业版`, and `Studio 工作室版` rather than falling back to `当前不可用` based on current-plan level mapping.
- Billing subscription status rule: the billing page must derive the top-right current plan, subscription status, and current-plan disabled button from one `/api/billing/summary.plan` display state; `Free`, `Starter`, `Demo`, and recharge-only values are not active subscriptions and should show `未订阅`, while subscribed visible plans disable their own card button with `您已订阅该套餐。`.
- Billing upgrade-only rule: subscribed users can only move to a higher visible subscription tier; lower-tier/downgrade subscription cards must keep fixed layout but disable their button and show downgrade-unavailable copy.
- Billing price source rule: subscription prices and credits must match the real Wallyt QR/order amount; current membership credit rule is HKD 1 = 13 credits. Visible plans are Lite 19/51/174, Pro 89/240/817, Studio 139/375/1276 for monthly/quarterly/annual. Hidden backend-recognized plans are Creator 179/483/1643 and Business 1599/4317/14679. Do not reintroduce the old high display prices 109/499/929 or monthly-credit based bonus calculations.
- Wallyt payment rule: 威富通支付 is integrated through backend-only env vars `WALLYT_DOMAIN_URL`, `WALLYT_MCH_ID`, `WALLYT_SIGNATURE_KEY`, `WALLYT_NOTIFY_URL`, and optional `WALLYT_WX_APP_ID`; never commit merchant secret values, and verify callbacks through `/api/billing/wallyt/callback` before marking admin-store billing orders paid.
- Wallyt reconciliation rule: 威富通 XML 解析、下单签名、回调验签、主动查询、异常订单事件、右上角支付告警和专项回归测试统一由 `server/wallyt-payment.ts`, `server/index.ts`, `server/admin-store.ts`, and `scripts/verify-wallyt-payment.mjs` 覆盖；本地 `.env` 可加载真实商户配置但不要提交。
- Admin order reconciliation rule: `/api/admin/orders/:orderId` returns order detail with payment events, credit ledger entries, notes, refund events, feedback, audit entries, and a reconciliation timeline; admin order actions include notes, confirmed manual reissue, and confirmed refund with credit clawback.
- Admin refund flow rule: refund events include money-flow nodes for request, credit clawback, provider refund submission, provider processing, and user funds arrival; the current refund step is visible in the order detail panel and regression-tested in `pnpm verify:admin`.
- AI billing policy rule: AI usage billing is controlled by the editable admin policy table saved through `/api/admin/ai-billing-policies/save`; successful AI tasks use the saved capability policy plus the user's membership discount, then write AI task records and credit ledger deductions. Cost/margin reports are estimated platform cost, not real provider invoice reconciliation.
- AI billing coverage rule: server AI usage accounting now covers text/image generation, background removal, HD enhancement, watermark removal, smart background, OCR, image edit, object erasure, image expansion, and generic `/api/ai/orchestrate`; failed authenticated tasks write admin alerts without deducting credits.
- Admin production-readiness rule: `server/admin-store.ts` now uses an `AdminDataRepository` boundary with JSON as the MVP adapter and `ARTX_ADMIN_DATA_BACKEND=json`; `docs/admin-database-schema.md` defines the target Postgres schema, and `pnpm verify:admin` runs the admin regression checks against a temporary data directory.
- Admin production-security rule: auth now enforces session expiry, login failure lockout, last-super-admin protection, self-disable protection, and high-risk admin write confirmations for AI billing policy changes and large credit adjustments; `pnpm verify:admin` includes these regression checks.
- Admin test-account issue UX rule: the account-management issue form defaults its expiry to 30 days ahead, prevents duplicate submits, and must show success or failure directly below the confirm button; fetch errors must never fail silently.
- Canvas persistence rule: real user projects use `canvas-*` IDs and must keep the latest edited nodes/edges in local storage; non-real test canvas states can be reset without touching user projects.
- Image generation recovery rule: text-to-image tasks with a `generationId` are also started on the backend `/api/images/tasks` background endpoint, so closing the browser can still recover completed or failed results when the user returns.
- Derived AI image recovery rule: canvas-derived AI image tasks such as smart product image, image edit, annotation edit, eraser, expansion, background removal, HD enhancement, watermark removal, element editing, and vectorization must persist a `backgroundTaskInput` with capability/operation and source image data; reload recovery must use that backend task input and must not restart source-image edits as plain text-to-image.
- Canvas assistant composer rule: image reference chips and annotation reference chips are ordered prompt segments; drag reordering must preserve the same order in both displayed prompt text and the reference image array sent to AI.
- Eraser AI rule: canvas eraser strokes call PicWish/佐糖 image object removal through `server/image-generation.ts#eraseWithPicWish`, using the official remove-objects `POST /api/tasks/visual/watermark` and `GET /api/tasks/visual/watermark/{task_id}` flow. It supports `sync=0` and `sync=1`, `image_file` or `image_url`, `mask_file` or `mask_url` or `rectangles`, provider task ID tracing, frontend transparent-hole masks, and white-remove/black-protect masks matching PicWish masked removal.
- Eraser isolation rule: the eraser path must not use local blur fallback, old masked compositing, or old result-guessing heuristics. If PicWish inpaint fails or returns no image, surface the failure instead of fabricating a blurred result; do not change outpaint, annotation, smart background, HD, layer editing, watermark, or background-removal paths when tuning eraser.
- PicWish logging rule: all PicWish/佐糖 calls must log server-side `[picwish]` JSON events for request, created task, polling, success, failure, and result download. Logs may include task type, endpoint, taskId, state, progress, duration, dimensions, and errors, but must never include API key values.
- PicWish response ID rule: all successful PicWish/佐糖 image capabilities must include provider task IDs in API responses as `providerTaskId` and, when multiple PicWish tasks are involved, `providerTaskIds`, so browser-side verification and `docs/dev-log.md` can trace each call without relying only on Render logs.
- Watermark removal rule: the image toolbar `去水印` command calls `client/src/lib/ai.ts#removeImageWatermark`, `POST /api/images/remove-watermark`, and PicWish/佐糖 `watermark` visual task; it must stay separate from eraser `inpaint`.
- Gemini image model rule: selectable Gemini/Nano Banana image models are exposed through `IMAGE_AI_MODELS` and routed as chat-compatible image models in `server/image-generation.ts`, so they avoid the unsupported Images API path.
- BKEEL image generation rule: `https://token.bkeel.com/v1/images/generations` returns an async `task_id`; image generation must poll the task result instead of expecting a synchronous `data[0].b64_json` response.
- Font design dialog layout rule: the panel must sit horizontally centered under the top canvas toolbar with 16px visual spacing, avoid horizontal scrolling, keep all style category labels/descriptions visible, keep every category preview image visible as a real proportional card instead of thin strips, and fit common browser heights with vertical-only internal scrolling.
- AI client routing rule: GitHub Pages builds must never send AI requests back to the Pages frontend; `client/src/lib/ai.ts` falls back to `https://backstage.artxsd.com`, and text-to-image requests with `generationId` should fall back to direct orchestration if the background task endpoint returns HTML/non-JSON.
- Smart background dialog layout rule: the panel must sit horizontally centered under the top canvas toolbar with 16px visual spacing, may use a wider content-led width, must keep upload, style, prompt, ratio, resolution, custom size, cancel, and create controls visible through vertical-only internal scrolling, and must not overflow the browser viewport.
- Smart background output rule: when product image, background reference image, prompt, ratio, and resolution/custom size are provided together, the reference image is only a style/lighting/material/background guide; the prompt still controls the specific requirements, and the returned bitmap must be normalized to the selected output size/aspect ratio with no black bars or empty gutters.
- Smart background canvas rule: when a user uploads both a product image and a background reference image, clicking create must place both source images onto the canvas before generation, then generate the result beside them; the API request must pass explicit output `customWidth/customHeight` derived from the selected ratio and 2K/4K/custom size.
- Smart product upload slot rule: after a product image or background reference image is submitted in smart product/background panels, the slot must expose explicit replace and delete controls; deleting the product image disables generation, while deleting the background reference only removes the optional style reference.
- Smart commerce product identity rule: generated smart commerce/product images must keep the uploaded product image as the only product subject. PicWish first extracts it into a protected transparent layer; platform/template prompts may control only the separately generated background. Sharp then composites the protected layer with a contact shadow, so no background model can replace the product category or invent unrelated goods.
- Smart commerce fusion rule: the protected product composite must derive its contact shadow from the lower alpha silhouette, never a fixed ellipse. Sample the generated floor near the contact line for bounded product brightness matching; preserve the cutout alpha and product pixels rather than regenerating the subject.
- Smart commerce reference-model rule: smart product backgrounds must call `gpt-image-2` first with only the optional background reference image, then retry `gemini-3.1-flash-image` when Image2 fails or returns no image. Both background routes preserve selected 2K/4K output normalization; if both fail, use PicWish `r-background` from the uploaded product as the final fallback.
- Smart commerce prompt-history rule: submitting an intelligent product-image generation must append one user message through `canvas-assistant-external-message`, containing only that generation's original user input. The exact composed generation prompt, platform configuration, and output specification remain internal to the generation request and must not appear in the user-visible canvas assistant history.
- Smart commerce template coverage rule: `shared/cross-border-commerce-agent.ts` owns market/platform/placement/category/template compatibility. Every active market-platform-placement-category combination must resolve to at least one template; Qatar and Kuwait remain unavailable until reviewed platform packages exist. The client must repair selections to a compatible placement and template rather than surfacing an empty-template error.
- Smart product image rule: the former user-facing smart background flow is now labeled `智能产品图`; it reuses `client/src/lib/social-media-presets.tsx` for platform cover sizes, grouped as 电商类 / 社媒平台类 / 内容/视频平台类, and can generate up to 9 images while keeping the internal `smart_background` capability key for billing/log compatibility.
- Smart product image platform style rule: the middle style selector in `ProductBackgroundDialog` is now a `主流电商平台` selector with platform cards for 亚马逊, Shopee, TikTok, Lazada, 抖音, 小红书, 淘宝, and 京东; detailed platform style prompts should be imported later from the user's MD specs instead of hard-coded as final rules.
- Cross-border commerce visual Agent rule: `shared/cross-border-commerce-agent.ts` is the source of truth for versioned market packages, active platform mappings, placement sizes/safe areas, six product categories, five template modes, and creative risk rules. Frontend selectors should call `/api/cross-border-commerce/markets`, `/risk-check`, and `/compose`; do not hard-code platform specs in UI. Qatar/Kuwait currently keep Gulf risk packs but default-hide ops-review platforms until operations verifies availability. Generated contexts write trace records to `.artx-data/cross-border-commerce-generations.json`.
- Ecommerce platform image playbook rule: `docs/ecommerce-platform-product-image-playbook-2026.md` records the 2026 visual/spec/data-access playbook for Amazon, Shopee, TikTok Shop, Lazada, Douyin, Xiaohongshu, Taobao/Tmall, and JD. Use it as the planning source for platform-specific product-image templates, but re-verify official seller-center specs and authenticated growth-data availability before productizing.
- Cross-border commerce frontend handoff rule: `docs/cross-border-commerce-frontend-implementation-handoff.md` is the implementation handoff for adding the homepage Shopping/ecommerce analysis module and productizing `/cross-border-commerce` into a platform + country/region + category + trending image style generation workflow.
- Cross-border commerce frontend master brief rule: `docs/cross-border-commerce-shopping-frontend-master-brief.md` merges the frontend implementation handoff and 2026 platform image playbook into one task-readable brief; new tasks should prefer this file when implementing the Shopping homepage module and `/cross-border-commerce` workflow, including the required Shopee / Indonesia validation path.
- AI capability safety rule: before adding a new AI capability, if the implementation may touch or regress already tuned AI capabilities such as annotation, outpaint/expand image, smart background, eraser, background removal, HD, watermark removal, layer editing, or image editing, warn the user first with the affected capability list and regression risk before changing those paths.
- Quick image edit rule: the image node quick edit / smart edit flow must call `editImageWithPrompt` with the selected/latest image as the target canvas, preserving subject identity, pose, silhouette, composition, aspect ratio, and unmentioned regions; it must not call pure `generateAiImages` for local edits because that can regenerate a similar but different person/object and break later background removal.
- Home login remember-password rule: the homepage “记住密码” option must not store raw passwords in cookies, localStorage, logs, or project memory; only remember the username/browser autofill preference and rely on native password-manager autocomplete for the actual password.
- Home auth credential UI rule: after a user is authenticated, the home page must not keep the login/password form mounted in hidden DOM, because browser password managers can still trigger identity confirmation UI from hidden forms. For multi-account login suggestions, keep native `autocomplete` attributes on visible unauthenticated fields and let the browser render its own credential dropdown; do not call `navigator.credentials.get` from the home page because it can open a large account chooser instead of the input-level browser UI.
- AI image regenerate rule: AI-generated image nodes expose a 60px green hover button labeled `再次生成`; successful nodes rerun into a new nearby image node, while failed nodes retry in place using the original prompt/model/ratio metadata.
- Canvas download/export rule: selected-image download, batch download, and artboard export must read remote images through a Blob/proxy fallback before drawing to canvas, otherwise cross-origin sources can make download/export fail or silently skip layers.
- AI expansion rule: `/api/images/expand` must use PicWish/佐糖 `POST /api/tasks/visual/advanced-image-expand` and `GET /api/tasks/visual/advanced-image-expand/{task_id}` through a dedicated expansion path, not the eraser/inpaint path; frontend expansion should send the enlarged canvas with transparent blank extension pixels plus a same-size mask, and returned expansion results must include provider task IDs before writing back to canvas.
- Multi-platform cover rule: the canvas multi-platform cover panel stores crop transform per selected platform preset; the crop icon only appears after a preset is selected, and export must read `payload.transforms[preset.id]` so each output keeps its own crop.
- Watermark removal API rule: automatic image watermark removal must call PicWish/佐糖 `/api/tasks/visual/external/watermark-remove` with the source image field named `file`; do not route it to masked object removal `/api/tasks/visual/watermark`, which requires a mask and fails with `mask image does not exist`.
- External website image drag rule: canvas drag-drop from external websites should parse image URLs from `text/html`, `text/uri-list`, `text/plain`, `DownloadURL`, Firefox URL types, image/link/meta/video poster/lazyload/srcset fields, then fetch remote images through the existing image proxy and store them as DataURL asset nodes; keep clipboard paste behavior separate.
- External drag browser quirk rule: some browsers expose only `Files` during `dragover` for external webpage image drags, so the canvas must still `preventDefault` and wait for `drop` to read `text/html`/URL payloads; otherwise the drop event never reaches ArtX.
- External page image proxy rule: `/api/images/proxy` should support dragged webpage URLs, not only direct image URLs; for Huaban `huaban.com/pins/:id`, resolve the image through `https://api.huaban.com/pins/:id`, and for generic pages parse common `og:image`, `twitter:image`, `image_src`, `img`, `srcset`, video poster, lazyload attributes, CSS `url(...)`, JSON/script image fields, and common image CDN URLs before failing.
- External drag broad format rule: when expanding third-party website drag support, read every string `DataTransferItem` on drop, not just standard `getData("text/html")`; parse escaped/protocol-relative/data-image URLs, prefer larger `srcset` candidates, and keep `/api/images/proxy` able to resolve HTML/JSON-LD/script image fields. Huaban pin API requires a trailing slash, `https://api.huaban.com/pins/:id/`, otherwise it can return 405.
- External drag Tencent/Huaban rule: Tencent News article URLs can resolve to extensionless `gtimg`/`qq.com`/`shiply-cdn.qq` image CDN candidates, while Huaban pin URLs need browser-like API headers and both trailing-slash/no-slash endpoint fallbacks; validate with `/api/images/proxy` returning a real image content type.
- External drag Zcool rule: Zcool work pages such as `zcool.com.cn/work/*.html` expose usable `og:image` and `img.zcool.cn/community` assets; paste should reuse the same rich HTML/plain-text extraction as drag-drop, and local validation should use a full backend server rather than pure Vite frontend.
- External drag local proxy rule: when testing webpage image drag locally on the full production server such as `localhost:5018`, image proxy requests must use the same local origin even if `VITE_API_BASE_URL` points to `https://backstage.artxsd.com`; otherwise newly fixed local proxy parsers are bypassed and drag-drop appears unchanged.
- External drag Chrome dedupe rule: Chrome can expose the same dragged webpage image as multiple `Files` entries while Safari may expose only one source; image preview pages may expose both a thumbnail and the original image. Dedupe URL-sourced drops by a dimension-independent visual fingerprint; for file-sourced drops that also include webpage string payloads, treat it as one external webpage drag and keep only the largest source pixel area before adding canvas nodes. Do not apply this one-image clamp to local Finder multi-file drops.
- External drag Chrome file-only dedupe rule: Chrome webpage image drags may arrive as multiple `Files` entries without `text/html`/URL string payloads; if the files are same-named image files or browser-generic image names, treat them as duplicate browser candidates and keep only the largest source pixel area.
- External drag Chrome cross-source dedupe rule: Chrome webpage image drags can expose the same image through both `Files` and webpage string/HTML URL payloads in a single drop; merge both candidate node sets before dedupe, then keep the largest single source for webpage drags while leaving local Finder multi-file drags untouched.
- External drag URL-only dedupe rule: Chrome may expose webpage image drags as URL/HTML candidates only, with thumbnail and large variants but no `Files`; URL-only canvas drops should also keep a single largest image candidate.
- Visible asset source rule: image copy/paste, AI edit/generation, annotation edit, crop/rotate, multi-platform export, selected-image download, batch download, and artboard export should read the current visible asset source by baking `assetAdjustmentPreview` or `assetAdjustments` into a DataURL before passing it onward; this prevents brightness/highlight edits from reverting to the original bitmap.
- Visible asset baking rule: when a canvas image has visual adjustments, all downstream operations must consume the baked visible pixels, not the original `localSrc`; this includes bottom/right AI reference chips, image-generator references, cross-canvas copy/paste, external URL paste normalization, single download, batch download, artboard export, and PSD layer export. Remote images should be read through Blob/proxy fallback before baking to avoid CORS reverting to the original.
- Canvas keyboard copy/paste rule: Ctrl/Cmd+C for selected canvas nodes writes to ArtX's internal cross-canvas clipboard, and Ctrl/Cmd+V must prefer that internal clipboard before showing system-clipboard image errors; if visible-image baking is still pending, paste should wait for that pending copy task before inserting nodes.
- Canvas assistant composer rule: image/annotation reference chips must keep adjacent empty text insertion slots so users can click before or after a chip to type; composer placeholder text should show only when there are no chips and no user text, and composer scrolling should depend on content overflow after the user-adjustable input height.
- Canvas assistant inline composer rule: bottom-right prompt text and image/annotation chips must render as one inline token flow, not separate multi-line textarea blocks; text slots stay single-line, empty text slots must occupy 0px, typed text width should be computed during React render from measured text pixels plus padding so the first typed character remains visible, chips can be mixed between text fragments, automatic wrapping should use about 2px row gap, rows must stay flex-start aligned so line spacing does not change with composer height, and placeholder/default text must stay top-aligned after the composer is resized taller.
- Canvas assistant Backspace rule: when a mixed prompt text segment is backspaced down to zero characters, keep the text segment alive and focused so the user can continue typing there; do not let the browser remove the editable field or break the inline chip/text flow.
- Canvas assistant skill token rule: when a Skill is selected, the bottom-right mixed prompt input must show the active Skill as one blue inline token; deleting that token cancels `activeSkill`, so generation must not use the Skill again until the user selects it again.
- Canvas assistant prompt scrollbar rule: bottom-right mixed prompt input must never show a horizontal scrollbar when text and image/annotation chips wrap; keep horizontal overflow hidden, hide visible scrollbars on the prompt shell, and let text/chips wrap inside the available width.
- Canvas assistant image chip preview rule: bottom-right image reference chips show their preview only after 1s hover or a single chip click; the preview is portaled above the full chip with a 2px gap and must stay above other UI layers. The workspace new-canvas dashed card stays square whether or not projects exist.
- Canvas assistant reference identity rule: bottom-right composer image tokens must synchronize with the current `referencedAssets` list instead of only appending new selections; generated-image backup activation must validate image source as well as `nodeId`, and restore with a unique node id if a stale backup id points to a different canvas image.
- Canvas visual-node context menu rule: after a visual node is selected by left click, the floating toolbar must intercept right-click and open the same `NodeContextMenu` as right-clicking the node, preventing the browser/system context menu while preserving copy, paste, note, and delete actions.
- Canvas assistant skill token rule: selecting a Skill from the canvas composer must synchronously insert or replace the blue inline skill token as the first composer segment, with no editable text slot or visual left gap before it; text/image/annotation input can only appear to the right, and deleting that token must still clear `activeSkill`.
- Canvas assistant skill generation rule: image and annotation reference tokens with empty or whitespace-only `src` must be filtered out before Skill generation, and completed generation events must only accept non-empty image `src`; otherwise show failure instead of creating a broken-image node.
- Canvas Skill generated image sizing rule: Skill-generated images must use the returned image's real dimensions to fit the canvas node frame, and stored `/uploads` generated images should return measured width/height; do not keep a fixed requested-ratio frame when the actual image aspect differs, because it creates top/bottom or left/right blank bars.
- Skill smoke-test rule: local full-server Skill generation requires configured image-generation env such as `AI_IMAGE_API_KEY`; without it, `/api/images/tasks` can only verify the failure path and returns `Missing AI_IMAGE_API_KEY`, so real Skill image output must be tested against a configured backend.
- Tencent Cloud gray migration assets: use `pnpm run package:tencent-cloud` to build a small gray release package under `output/tencent-cloud/`; deploy helpers live in `deploy/tencent-cloud/`, and `pnpm run verify:tencent-cloud` runs health, image proxy, billing config, optional Skill image, and optional Wallyt callback smoke checks without printing secrets.
- Tencent Cloud gray backend deployment: server `43.161.241.133` has an isolated gray backend at `/opt/artx-gray-backend/current`, systemd service `artx-gray-backend.service`, local port `127.0.0.1:3002`, data `/var/lib/artx-gray`, and uploads `/var/lib/artx-gray/uploads`; existing `/opt/artx`, `artx-admin.service`, port `3001`, and `/var/lib/artx` were left untouched. Public HTTPS entry `https://backstage.artxsd.com` replaced Render for test/gray backend, and full smoke passed for health, image proxy, auth, Wallyt callback, Skill image generation, and generated image download.
- Test/gray API routing rule: frontend-side API calls, AI, Auth, Billing, and image API defaults must point to `https://backstage.artxsd.com`; do not use `https://artx-test.onrender.com` for test/gray API routing. Keep Render only as a backup unless the user explicitly asks to restore it.
- Tencent Cloud auto-deploy rule: `.github/workflows/deploy-tencent-cloud.yml` deploys `feature/interaction-framework` or `test/feature/interaction-framework` to `https://backstage.artxsd.com` via SSH, using the GitHub secret `TENCENT_CLOUD_SSH_PRIVATE_KEY`; default smoke is non-consuming, and manual `run_full_smoke=true` runs Skill image plus Wallyt callback checks.
- Test environment publish rule: when the user says "提交到测试环境" or "发布到测试环境", the required path is local code commit -> push to GitHub branch `feature/interaction-framework` -> GitHub Pages / test frontend redeploy -> sync the Tencent Cloud frontend page -> verify both online frontend versions. Unless the user explicitly says to generate a local test link, do not bypass GitHub. Tencent Cloud replaces the old Render backend and also serves a frontend page at `https://backstage.artxsd.com`; all frontend and backend code changes must still be committed and pushed to GitHub first, then Tencent Cloud frontend must be synchronized from that pushed code so it shows the same latest effect. The fixed GitHub Pages test frontend URL is `https://09beedesign-star.github.io/artx-test/`, and the fixed Tencent Cloud frontend URL is `https://backstage.artxsd.com/`. Verification must confirm both `https://09beedesign-star.github.io/artx-test/deployment.json` and `https://backstage.artxsd.com/deployment.json` have `shortCommit` matching the pushed commit.
- Browser smoke-test rule: `pnpm verify:live-page` uses the project Playwright dependency to open `https://backstage.artxsd.com/` in headless Chromium, collect page/console errors, and fail if React #185 or maximum-update-depth text appears. If Chromium is missing locally, run `pnpm exec playwright install chromium` once.
- Inspiration recommendation data rule: the 62 public PromptBase references were removed because their public pages expose preview images and descriptions, not verified prompts for those exact displayed images. Do not re-import PromptBase descriptions or link-only rows into `/inspiration`; the current replacement dataset is 900 GitHub curated prompt-image pairs from YouMind GPT Image 2 / Nano Banana Pro, WeShop GPT Image 2, PicoTrex Nano Banana, and Awesome GPT Image 2 Gallery.
- Inspiration verified prompt rule: user-visible `/inspiration` external data must use `verifiedPromptOnly=1`; only image records with a public image URL and that same item's displayed generation prompt qualify. Civitai, Lexica, and approved GitHub curated prompt-image galleries are engineered verified sources; source URLs/sites/license notes must stay server-side instead of being returned to the frontend.
- Inspiration detail privacy rule: `/inspiration` may keep source metadata internally for attribution and debugging, but the user-facing detail modal should not expose a highlighted “查看来源” link or otherwise send users out to external prompt/source sites; keep the in-product prompt display and copy flow instead.
- Inspiration source-hiding rule: `/inspiration` should not show model-source filters, per-card author/source labels, external source-site chips, source-stat cards, or license/source notes in the user-facing UI. Keep source metadata internal only for loading, attribution review, and debugging.
- Inspiration category navigation rule: `/inspiration` filters should present a two-level hierarchy. The first row is the top-level tab set with `全部分类` plus available groups; draw a horizontal divider under the first-level tab row instead of writing visible `一级分类`/`二级分类` labels. The second row below that divider is the scoped subcategory tab set for the selected top-level group, with `全部` as the reset option. Keep model/source filtering visually separate from these two category levels.
- Production build runtime rule: do not let `vite-plugin-manus-runtime` inject Manus preview/runtime code into production builds; if online `deployment.json` is current but Playwright shows `#root` with no children, inspect the served HTML for `manus-runtime`, blocked external font/CSP errors, and whether the main module script is requested. `dist/public/index.html` must keep a direct same-origin `type="module"` Vite entry after `#root`; do not route startup through a loader that depends on `document.currentScript` or stylesheet load events, because either can leave the root permanently empty.
- Home auth credential UI rule: after a user is authenticated, the home page must not keep the login/password form mounted in hidden DOM, because browser password managers can still trigger identity confirmation UI from hidden forms. For multi-account login suggestions, keep native `autocomplete` attributes on visible unauthenticated fields and let the browser render its own credential dropdown instead of building a custom account chooser.
- Tencent Cloud static frontend rule: `backstage.artxsd.com` should let Nginx serve `/assets/`, `/entry-loader.js`, `/deployment.json`, and `/inspiration-images/` directly from `/opt/artx-gray-backend/current/dist/public` with gzip/cache, while proxying `/api/` and `/uploads/` to `127.0.0.1:3002`; ensure the Nginx worker user `www-data` is in the `artx` group so it can read `root:artx` release files.
- Tencent Cloud public-host rule: `backstage.artxsd.com` is the gray website and API host, not the admin host. Packaging must leave `VITE_ADMIN_HOST` empty unless an independent admin subdomain is explicitly configured; otherwise `/` redirects to `/admin-prototype`.
- Tencent Cloud gray admin-route rule: while `backstage.artxsd.com` is not a dedicated admin host, its explicit `/admin-prototype` route must remain available for test administrators; keep this route exception separate from the root-path redirect decision.
- Production release rule: when the user says "发布正式环境", first verify `https://backstage.artxsd.com`, then release to `https://www.artxsd.com`; keep production code/service/data/uploads isolated from gray, preferably `/opt/artx-prod/current`, `artx-prod.service`, `127.0.0.1:3003`, `/var/lib/artx-prod`, and `/var/lib/artx-prod/uploads`; follow `docs/production-release-flow.md`.
