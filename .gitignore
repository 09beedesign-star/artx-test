# Dependencies
**/node_modules
.pnpm-store/

# Build outputs
dist/
build/
*.dist

# 文字编辑链路的调试产物与本地预览输出
# （调试落盘默认关闭，需显式设置 TEXT_EDIT_DEBUG_DIR / MEITU_DEBUG_MASK_DIR 才会生成）
debug-mask-*.png
debug-cleaned.png
debug-drawn.png
debug-composited.png
outputs/text-edit-preview/

# 一次性排查探针脚本。
#
# 这些脚本是定位具体缺陷时临时写的（擦字残留量化、OCR 框错位复核、
# 逐区域像素差异比对、VOD 模型版本枚举等），有很强的环境依赖：
# 需要本地测试海报、运行中的 8077 擦字引擎、或真实 VOD 凭证，
# 换台机器基本跑不起来，进仓库只会变成噪音。
#
# 注意这里只忽略 probe-* 与 e2e-*，**不包括 verify-*** ——
# scripts/verify-model-env.mjs 这类是部署流水线要调用的正式校验脚本，必须入库。
scripts/probe-*.mts
scripts/probe-*.mjs
scripts/e2e-*.mts
# 依赖本地测试海报的一次性场景验证脚本（判据写死了特定图片的区域坐标）。
scripts/verify-local-erase-scene-c.mts

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# 忽略所有 .env 副本与变体（如 (1).env、backup.env），避免误提交泄露密钥
*.env

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock
.artx-data/

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt

# Gatsby files
.cache/

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Database
*.db
*.sqlite
*.sqlite3

# Webdev artifacts (checkpoint zips, migrations, etc.)
.webdev/

# Manus version file (auto-generated, not part of source)
client/public/__manus__/version.json
.project-config.json
