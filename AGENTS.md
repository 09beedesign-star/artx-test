# Project Rules

- After every change to AI capability parameters, model routing, API keys, base URLs, or AI-related backend/frontend integration, also verify and fix the register/login panel error handling before finishing. This includes checking that auth fallback behavior, registration prompts, login prompts, and visible error messages still work correctly after the AI change, so the user does not need to request this separately each time.
